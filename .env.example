# Optional dev shortcuts — login screen overrides these when credentials are saved in localStorage
# Backend API URL (default: http://127.0.0.1:8000)
VITE_API_BASE_URL=http://127.0.0.1:8000

# Bearer token matching JARVIS_API_TOKEN on the backend
VITE_JARVIS_TOKEN=
